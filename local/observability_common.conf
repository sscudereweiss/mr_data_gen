# Shared Splunk Observability settings for all mr_data_gen scripted inputs.
# Set host_name on deploy to match the Splunk OTel Collector infra host identity
# (run `hostname -f` on SA Lab and use that exact value).
[observability]
environment = itsi_demo
log_correlation = true
host_name =
